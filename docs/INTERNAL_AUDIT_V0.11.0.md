# Internal Audit — V0.11.0 Release Stress Audit

## Scope

V0.11.0 is a release-audit increment, not a feature increment.

Production-source delta relative to V0.10.0.1:

1. `BatchRenamer.Transaction/RenamePlanIntegrity.cs`
   - nullable-flow capture only; fixes the two Release CS8602 warnings observed during real `dotnet publish`;
   - no validation rule, error code, path comparison rule, or transaction contract changed.
2. `BatchRenamer.App.csproj`
   - version metadata only.

No mutation/recovery implementation changed:

- `TransactionPhase1Executor` unchanged;
- `TransactionPhase2Executor` unchanged;
- `TransactionRollbackExecutor` unchanged;
- `JournaledRenameMutationFileSystem` unchanged;
- `TransactionExecutionOrchestrator` unchanged;
- `TransactionRecoveryOrchestrator` unchanged;
- `TransactionUndoOrchestrator` unchanged;
- `TransactionNewExecutionCoordinator` unchanged;
- `TransactionUserUndoCoordinator` unchanged.

## Release tooling added

- `BatchRenamer.ReleaseStressTests`: Windows-only real filesystem scale harness.
- `run_release_stress.py`: standalone stress runner.
- `run_v1_release_gate.py`: strict one-command final gate.
- `publish_portable.py`: release publish now promotes warnings to errors by default; `--allow-warnings` is diagnostic only.

## Stress safety boundary

- private sandbox under `%TEMP%/BatchRenamer.ReleaseStress/<GUID>`;
- source/target names are generated by the test harness;
- no user folder input accepted;
- all namespace mutation still routes through production `TransactionNewExecutionCoordinator` / `TransactionUserUndoCoordinator` and `SystemRenameMutationFileSystem`;
- successful runs automatically remove the sandbox;
- failed runs preserve it.

## Static status

- Python release scripts: `py_compile` PASS.
- ZIP/source boundary scan: production mutation core unchanged except nullability-only integrity fix.
- Current environment lacks Windows/.NET 10, so V0.11.0 remains `STATIC_CHECKED` until the strict Windows release gate is run.
